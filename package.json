{
  "name": "urban-jungle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "find . -name \"node_modules\" -exec rm -rf '{}' + && find . -name \".build\" -exec rm -rf '{}' +"
  },
  "workspaces": {
    "packages": [
      "app",
      "shared",
      "functions"
    ],
    "nohoist": [
      "**/prettier-plugin-organize-imports"
    ]
  }
}
